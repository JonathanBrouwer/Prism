use crate::jonla::jerror::Span;
use crate::lambday::lambday::LambdayTerm;

impl LambdayTerm<Span, usize> {
    // pub fn type_check(self) -> LambdayTerm<(Span, LambdayTerm<Span, usize>), usize> {
    //
    // }
}