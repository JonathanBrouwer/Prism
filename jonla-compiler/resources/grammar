rule block:
    g :: b <- "grammar" "{" g:@grammar "}" ";" b:block
    s :: b <- s:stmt ";" b:block
    [] <- ""

rule stmt:
    Let(e) <- "let" e:expr
    Do() <- "do"

rule expr:
    Block(b) <- "{" b:block "}"
    X() <- "x"
    Y() <- "y"

rule layout = [' ' | '\n']
