let a = Type;
adapt grammar {
    adapt rule expr {
        adapt group base {
            a <- "$";
        }
    }
};
let b = Type -> Type;
adapt grammar {
    adapt rule expr {
        adapt group base {
            $ <- "%";
        }
    }
};
let c = Type -> Type;
%

