let => #0;
let => (let #0: Type;
#0);
let => => #1;
let => => #0;
let => (let #0: Type;
=> (let #0: Type;
#2));
let => (let #0: Type;
=> (let #0: Type;
#0));
let => (let #0: Type;
=> #0);
let => => (let #0: Type;
#0);
let => => (let #0: Type;
=> #3 #1);
(Type -> _) -> Type -> (let #0: Type;
_ -> (let #1;
_))