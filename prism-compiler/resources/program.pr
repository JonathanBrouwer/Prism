let test = (
    let a = Type -> Type;
    let x = Type;
    grammar {
        adapt rule expr {
            adapt group base {
                x <- "%";
            }
        }
    }
);
adapt test;
%