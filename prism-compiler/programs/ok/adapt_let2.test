### Input
let x = Type -> Type;
grammar {
    rule expr {
        group statement {
            let n = Type; x <- "tel" n:identifier;
        }
    }
    rule keyword {
        "tel";
    }
}

tel x


### Eval
Type -> Type

### Type
Type
