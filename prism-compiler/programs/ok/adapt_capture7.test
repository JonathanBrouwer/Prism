### Input
let a = Type;
adapt_grammar {
    adapt rule expr {
        adapt group base {
            a <- "$";
        }
    }
}
let b = Type -> Type;
adapt_grammar {
    adapt rule expr {
        adapt group base {
            $ <- "%";
        }
    }
}
let c = Type -> Type;
%

### Eval
Type

### Type
Type
